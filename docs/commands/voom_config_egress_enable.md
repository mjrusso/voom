## voom config egress enable

Enable the explicit proxy attachment

### Synopsis

Validate and enable the configured proxy attachment. A running VM does not need to restart. Ordinary direct network egress remains available.

```
voom config egress enable <name> [flags]
```

### Options

```
  -h, --help   help for enable
```

### Options inherited from parent commands

```
      --output string   output format: text or json (default "text")
  -v, --verbose         enable verbose diagnostics
```

### SEE ALSO

* [voom config egress](voom_config_egress.md)	 - Configure an explicit proxy attachment
