version: "2"

linters:
  default: standard
  # `standard` enables: errcheck, govet, ineffassign, staticcheck, unused.
  enable:
    - revive

formatters:
  enable:
    - gofmt
    - goimports
  settings:
    goimports:
      local-prefixes:
        - github.com/mjrusso/voom
